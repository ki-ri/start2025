# データベース接続設定
DATABASE_URL=jdbc:postgresql://localhost:55432/demo
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=password

# データベース接続プール設定
DATABASE_POOL_SIZE=10
DATABASE_POOL_MIN_IDLE=2

# サーバー設定
SERVER_PORT=8089

# Tomcat設定（本番環境用）
TOMCAT_MAX_THREADS=200
TOMCAT_MIN_SPARE_THREADS=10
TOMCAT_ACCEPT_COUNT=100
TOMCAT_MAX_CONNECTIONS=8192

# ログレベル
LOG_LEVEL=INFO
APP_LOG_LEVEL=DEBUG

# Spring Profile
SPRING_PROFILES_ACTIVE=dev

# 本番環境用の例（application-prod.ymlを使用）
# SPRING_PROFILES_ACTIVE=prod
# DATABASE_URL=jdbc:postgresql://your-rds-endpoint:5432/production_db
# DATABASE_USERNAME=prod_user
# DATABASE_PASSWORD=secure_password_here
# DATABASE_POOL_SIZE=20
# DATABASE_POOL_MIN_IDLE=5
# SERVER_PORT=8080
# LOG_LEVEL=WARN
# APP_LOG_LEVEL=INFO
